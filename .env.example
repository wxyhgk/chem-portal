# 默认留空：前端走相对路径 /api/*，由 next.config.mjs rewrite 代理到后端
# （生产 Vercel 为 https，直连 http 后端会被浏览器拦截，故不要填 http 直连地址）
# 代理目标：next.config.mjs fallback http://CHEM_PORTAL_HOST:18081（本机 hermes 后端 :18081）
# 仅在需要直连本地后端调试时设置：
# NEXT_PUBLIC_API_URL=http://localhost:18080
