# 浏览器一律走同源 /api/*，由 next.config.mjs rewrite 代理到后端；不要让浏览器直连后端（会绕过登录）
# 服务端代理目标（rewrites 在 build 时固化，修改后需重新 build）：
# API_INTERNAL_URL=http://127.0.0.1:18081
# 全站 Basic Auth（生产环境必须设置 PORTAL_PASSWORD，否则返回 503；开发环境不设则不校验）：
# PORTAL_USER=chem
# PORTAL_PASSWORD=
